
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: bash entrypoint.sh
    volumes:
      - .:/app
      - static_volume:/static
      - media_volume:/media
    ports:
      - "8002:8002"
   
     
volumes:
  static_volume:
  media_volume:
